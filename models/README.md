# models/

Letakkan file model dan metadata di folder ini.

Contoh struktur yang umum:
- `model.keras` (atau `.h5`)
- `class_indices.json` (mapping index -> nama kelas)

Lalu sesuaikan kode loader di `app.py`.
